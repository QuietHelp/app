# Backend API (for session, matching, peer/global chat). With API Gateway, point this to the gateway.
NEXT_PUBLIC_API_URL=http://localhost:8080

# Chatbot proxy: used by Next.js /api/chat. In production with API Gateway, use the gateway URL so
# all API traffic goes through one entry (e.g. http://api-gateway:8080). Otherwise use chatbot direct (http://chatbot:8000).
CHATBOT_SERVICE_URL=http://localhost:8000
